# from tensorflow.keras.applications import VGG16, EfficientNetB5
# import tensorflow.keras as keras
# from tensorflow.keras.layers import *
# model = EfficientNetB5(weights=None,include_top=False, input_shape=(96,96,1))
# keras.utils.plot_model(model, to_file='dot_img_file.png', show_shapes=True)
#
# [print(i.shape, i.dtype) for i in model.inputs]
# [print(o.shape, o.dtype) for o in model.outputs]
# [print(l.name, l.input_shape, l.dtype) for l in model.layers]

